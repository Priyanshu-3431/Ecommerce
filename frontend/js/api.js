const API={token:localStorage.getItem('sv_token')||'',async call(path,opt={}){const headers={'Content-Type':'application/json',...(opt.headers||{})};if(this.token)headers.Authorization='Bearer '+this.token;const r=await fetch('/api'+path,{...opt,headers});let data={};try{data=await r.json()}catch{}if(!r.ok)throw new Error(data.message||'Request failed');return data},setAuth(data){this.token=data.token;localStorage.setItem('sv_token',data.token);localStorage.setItem('sv_user',JSON.stringify(data.user));},logout(){this.token='';localStorage.removeItem('sv_token');localStorage.removeItem('sv_user');location.href='/';}};window.API=API;
